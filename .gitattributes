data/** filter=lfs diff=lfs merge=lfs -text

# Frozen legacy snapshot, kept as a plain Git blob (not LFS) so pre-migration
# raw.githubusercontent.com URLs hardcoded into already-shipped consumers keep
# resolving to real content instead of an LFS pointer. Not regenerated by the
# pipeline; see data/public-hash-list-lfs.dat for the live, continuously
# updated file.
data/public-hash-list.dat !text !filter !diff !merge
data/public-hash-list.dat.sha256 !text !filter !diff !merge
